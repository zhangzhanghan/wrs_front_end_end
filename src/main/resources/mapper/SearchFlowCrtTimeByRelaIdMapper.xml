<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.mapper.SearchFlowCrtTimeByRelaIdMapper">
    <select id="searchFlowCrtTimeByRelaIdMapper" parameterType="String" resultType="String">
        SELECT crtTime FROM flowInfo WHERE relaId = #{relaId}
    </select>
</mapper>

